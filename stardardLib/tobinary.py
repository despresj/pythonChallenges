def count_bits(n):
    binary = bin(n)
    numList = [int(binary) for digit in str(binary)]
    bits = sum(numList)
    return bits
  
        
for i in range(100):
    bits = count_bits(i)
    print(bits)

for i in range(100):
    binary = bin(i)

binary = bin(10)

[binary for char in list(binary)]

nums = 0


    
print(nums)

